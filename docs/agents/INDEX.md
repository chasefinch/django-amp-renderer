# Agent Notes

Notes from past agent sessions for django-amp-renderer.

## Project Overview

Django middleware that applies AMP (Accelerated Mobile Pages) server-side rendering transformations. The core logic is in `django_amp_renderer/django_amp_renderer.py`.
